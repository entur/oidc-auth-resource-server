server:
  port: 9010

entur:
  auth:
    authorization:
      permit-all:
        matcher:
          patterns:
            - /unprotected
    tenants:
      environment: mock
      include: partner, internal